{
  "compilerOptions": {
    "jsx": "react-jsx", // JSX 코드 생성 설정: 'preserve', 'react-native', 혹은 'react'.
    "allowJs": true,    // 자바스크립트 파일 컴파일 허용 여부
    "target": "ES5",    // 사용할 특정 ECMAScript 버전 설정
    "outDir": "./dist", // 해당 디렉토리로 결과 구조를 보냅니다.
    "moduleResolution": "Node",               // 모듈 해석 방법 설정
    "lib": ["ES2015", "DOM", "DOM.Iterable"]  // 컴파일에 포함될 라이브러리 파일 목록 
  },
  "include": ["./src/**/*"],
  "exclude": ["node_modules", "dist"]
}